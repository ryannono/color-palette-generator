# Color Palette Generator Configuration

# Pattern source file path (relative to package root)
# The pattern file defines the source pallettes used for
# learning and deriving new color palettes.
# Default: patterns/default.json
PATTERN_SOURCE=patterns/default.json

# Default output color format
# Options: hex, rgb, oklch, oklab
# Default: hex
DEFAULT_OUTPUT_FORMAT=hex

# Default palette name when not specified
# Used as the base name for generated palettes
# Default: generated
DEFAULT_PALETTE_NAME=generated

# Default batch name when not specified
# Used as the group name for batch-generated palettes
# Default: batch
DEFAULT_BATCH_NAME=batch
